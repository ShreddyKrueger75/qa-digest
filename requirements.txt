# QA mode (default): diff-based frame selection + pointer localization
Pillow>=10.0
numpy>=1.24
# transcription
faster-whisper>=1.0
# only used with --no-dedup (plain scene sampling)
scenedetect>=0.6
